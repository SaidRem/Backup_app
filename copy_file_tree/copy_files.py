import os
import sys

ONE_MB = 2**20  # 1 Mb
HUNDR_MB = ONE_MB * 100  # 100 Mb 


def file_copy(orig_file, copy_to, file_size=HUNDR_MB):
    """
    Copy a file from 'orig_file' (path) to directory 'copy_to' (path).
    """
    pass


def file_tree_cp(orig_dir, copy_to):
    """
    Copy all dirs, subdirs and files in 'orig_dir'
    to 'copy_to' (path to empty dir).
    """
    pass

